{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["pet", "main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "opener:default"
  ]
}
